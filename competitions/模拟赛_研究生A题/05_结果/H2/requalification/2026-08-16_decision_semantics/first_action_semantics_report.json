{"check":"FIRST_ACTION_SEMANTICS","checker":"checker/h2_p3b_checker_v1.py check_rollout_kernel (REQUALIFIED: own frozen-head derivation + own expected first-action effects)","id":"FIRST_ACTION_SEMANTICS","impl":"main_model/h2_rollout/rollout_engine_v1.py _apply_first_action_if_dispatch / _start_frozen_head / wait_holds / h2_wait_wake; h2_policy_v1.py evaluate_decision_point(decision_head=...)","note":"candidate first actions FAIL CLOSED without their frozen decision context (never a silent no-op)","status":"PASS","tests":["ACT-01 START_HEAD records the frozen head's ACTIVITY_START at the decision time t","ACT-01B frozen-head mismatch fails closed (ValueError)","ACT-01C missing decision context fails closed (ValueError)","ACT-02 WAIT_EVENT holds the decision resource until the anchor (no head ACTIVITY_START in [t, anchor))","ACT-03B hold released at the anchor; H1 baseline resumes (frozen head served at the anchor)","ACT-03 START_HEAD vs WAIT_EVENT paths differ (B start 2 vs 5; different continuation)","ACT-04 PM_WITH_HEAD begins the preventive replacement; the head is served only after the calibration","ACT-05 PM_IDLE executes the replacement","ACT-06 H1_NOOP triggers no optional PM (age 0)","ACT-07 H1 baseline continues after the first step","ACT-08 PM candidate path differs from the H1 baseline path"]}
