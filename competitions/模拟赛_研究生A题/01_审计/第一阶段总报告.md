# 第一阶段总报告

> 冻结说明：本报告记录 G1 结束时的审计结论，不继续承载后续实时进度。A03 已重新签字为“单次无条件字面主情景、标准初测—重测链关键替代情景”；P6按“弱支配未证明、仅作操作约束”处理；Q3主情景不得宣称 `K=12` 弱支配。当前 Gate 和注册表只看项目根 `CURRENT_STATE.md`。

## 结论先行

- 题面共 3 页，无 A 题数据附件；表 1 只有 `lambda_1`–`lambda_4`，表 2 为空白结果表。
- 题型是“不完全观测可靠性 + 两驻留台位的并行 fork-join 测试 + 班历和设备老化调度”。A/B/C 可在同一装置上并行，已经推翻早期串行草案。
- 队伍已确认 A01–A09、B01–B19：不维修、固定观测核、E 只重做 E、最终结果级 PL/PW、Q3 设备完全重置，以及跨班保留全局 FCFS 队列与待重测逻辑状态。
- 固定 `alpha,beta` 的观测核已经确认；单次无条件差错口径升为题面字面主情景，标准初测—重测链降为关键替代情景，两者都须完整重跑。运输采用1h字面主情景，并以0.5h完全重叠为替代敏感性。
- V3补入终局四格闭式质量锚和240h前置换新；H2改为固定FCFS下的后验全程rollout，路线已经复审签字。H1/R1始终为最小基线与自动降级方案；后续实时阶段和 H2 准入状态见 `CURRENT_STATE.md`。

## 1. 题目与数据审计

### 已核验事实

1. 大厅容纳两个装置；A/B/C/E 各有一套专用资源。
2. A/B/C 都通过后才能进行 E；异常需重测，同一工序连续两次有效异常则退出。
3. A/B/C/E 正常测试时长为 2.5/2/2.5/3 h；设备校准时长、120/240 h 故障节点、问题率、测手差错率和班制均在题面给出。
4. 题面写“运出和运入各0.5h”；队伍确认顺序相加的1h完整周转为主情景，同一台位一出一进完全重叠的0.5h为替代情景。
5. Q2/Q3 均测试 100 台并输出 `T,S,PL,PW,YXB1–YXB4`；Q3 枚举七个 K；Q4 复用 Q3 分析工期因素。

### 文件审计

- 唯一题面输入：`00_题目/A题.pdf`，SHA256 `51EFA3628DF52C8CB66B7A51BB724627FCC505D680E80000AA10F16EEE9D5F11`。
- 队伍讨论证据：`01_审计/队伍讨论原稿_歧义确认_2026-08-12.pdf`，SHA256 `B4F70D947C56731499211BAF52AAB962CE2EC03406373BD845E01912C826A8D7`。
- B 题附件和旧材料不进入本题模型；工作区背景文章只用于方法规范，不迁移旧题结论。

## 2. 已确认的核心口径

| 主题 | 队伍主口径 | 证据边界 |
|---|---|---|
| A/B/C | 同一装置可并行占用 A/B/C；同类资源容量仍为 1；E 等三项全部通过 | 状态必须记录并发任务集合，不能再用单一当前工序 |
| 重测 | 不维修；真实状态不变；首次异常重测同工序，第二次异常退出；中断无结果 | 并行取消和同刻事件需在 G2 前写成唯一状态机 |
| 差错 | 每组固定 `alpha,beta`；字面主情景为单次无条件 `(1-q)alpha=q beta=e/2`，标准初测—重测链总体差错率为 e 且 FP/FN 各半作为关键替代 | 两种口径均先标定A/B/C，再传播`q_E`并标定E，随后完整重算质量、工期和七个K；不可行时不得截断造解 |
| E 与 lambda | E 异常只重做 E；D 一次生成；主表 lambda 为所有 E 真阳性事件的真实来源等权份额 | 假阳性不进主表；报告指向另列情景 |
| 设备 | 分段线性 CDF；年龄计实际测试片段；更换后调试；Q3 完全新初始化 | 分段常风险率作敏感性；Q3 不解释成 Q2 连续生产 |
| 目标 | 条件分离后最小化 `E[T]`，质量独立报告 | 路径级不变性失败则先查随机流/中断实现 |
| PL/PW | 最终结果级，均以 100 台为分母 | 事件级与条件率只作辅助，不替换主表 |
| 班历 | Q2 12 h；Q3 每日两班各 K；换班只更新`on_duty_squad`，全局队列、FCFS键及待重测状态跨班保留；班末不足禁启 | 禁启弱支配尚未证明，仅作为操作约束；主情景不同 K 的班界不嵌套，七点必须枚举。只有无缝续做退化对照才讨论 `K=12` 弱支配 |
| 运输 | 主情景为顺序换位1h；两个台位同一运输阶段可并行 | 替代情景0.5h，附加同一台位一出一进完全重叠假设；完整重跑 |

完整定义见 [队伍确认口径](队伍确认口径_2026-08-12.md) 和 [关键歧义与已确认口径](关键歧义与候选约定.md)。

## 3. 小问依赖与模型接口

| 问题 | 核心输出 | 下游复用 | 独立证据 |
|---|---|---|---|
| Q1 | 到达E残余状态、E阳性、lambda、终局四格与`E[S],E[PL],E[PW]` | 可靠性核、质量解析锚和随机工作量 | 闭式、16状态枚举、吸收链与独立质量oracle |
| Q2 | 单班计划、表 2 与事件日志 | 公共 DES、设备和指标层 | 1/2 台手算 + 日志重放 |
| Q3 | 七个 K 的配对比较、推荐计划和表 2 | 只替换班历、`on_duty_squad`标签和独立重置初态；不重建全局队列 | 七点全枚举 + 共同随机数 + B19跨队重测日志重放 |
| Q4 | 资源损失账本、配对反事实、少量交互和建议 | 不另建主模型 | 键控CRN、配对区间、建议—结果编号映射；并行小时不硬加成T |

## 4. 候选路线比较

| 路线 | 核心 | 优点 | 风险/闸门 | 定位 |
|---|---|---|---|---|
| H1/R1 | 有限状态可靠性核 + 连续事件 DES + 可解释规则/阈值/K 枚举 | 最透明、最稳、可完整复核 | 可能遗漏状态依赖派工价值 | **强制基线和自动降级路线** |
| H2 | H1 + 固定FCFS下基于可观察历史的后验生成 + 队首启停/预防更换全程rollout | 能处理班界和设备年龄下的非预知时机决策，且不越过队首或读取隐藏世界 | G3出口C24先核机会密度并冻结预算；后验、信息边界、墙钟或留出收益任一失败即删除 | **V3已签字、尚未运行的条件候选** |
| H3 | 场景随机规划/POMDP/大型 CP-SAT | 表面统一 | 状态树爆炸、题外风险参数、易泄漏未来信息 | 不作主线；CP-SAT只作小例精确基准或明确放松项的完美信息离线参照 |

创新不在“用了 DES/Markov/rollout”，而在本题特定的三点：差错标定的可识别性审计；终端质量—时间层的条件单向耦合；两驻留槽并行测试与设备再生状态共同驱动的非预知有限前瞻。任何创新都必须通过反例和消融，不靠模型名称加分。

## 5. 风险摘要

### G1 阻断项关闭情况

1. A03主次语义已由 G1 再评审重新签字：单次无条件为字面主情景，标准链为关键替代；并行事件规格、B18班末活动边界和B19换班队列语义均已签字，V3固定FCFS后验全程rollout路线复审已完成。
2. B05已签字：1h字面口径为主，0.5h完全重叠为替代敏感性；独立完整运行。
3. 检查注册表已冻结为`CR-V3.1`；这表示检查定义完成，并不表示任何G2/G3检查已实际通过。`CR-V3.0` 仅作历史映射。

### 必须披露的建模风险

- 若采用0.5 h，必须说明它不是原文直接结论，而是同一台位运出/运入完全重叠的附加解释；1 h 为题面字面。
- Q3 完全重置是独立新任务情景，不能用 Q2 末端设备年龄解释。
- lambda 是真阳性真实归因，不是所有阳性的测手报告方向。
- 主表 PW 是终态真实正常却退出，不等于事件级“发生过误判”。
- 班末禁启是队伍操作约束，其在更大策略空间中的弱支配尚未证明；不得把未经严格构造的机制说明写成反例证明。
- Q3主情景的班界随K改变、可行窗口不嵌套，因此不得由“总工作时长更多”直接推出`K=12`弱支配；该结论只属于无缝续做退化对照。

## 6. G2 最小可验证基线

G2已经授权，当前按 `CR-V3.1` 阶段索引执行 `C01`–`C06`、`C08`–`C12`、`C16`–`C21` 及 `C27` 文档部分；待执行时仍按低复杂度顺序推进：

1. 主情景先按单次无条件方程标定A/B/C，再由到达E状态标定E；关键替代按标准初测—重测链方程重复同一上游—下游过程。两种口径分别完整生成四格、`lambda`及后续工期输入；主实现与独立回代分离。
2. Q1闭式四格、16状态枚举、吸收链和质量oracle对拍，不手抄数值。
3. 关闭问题、差错和故障，建立支持 A/B/C 并行的连续事件引擎。
4. 用 1 台、2 台及“同刻两个阳性”手算事件表冻结批量结算与取消。
5. 独立检查器只读日志，复算资源容量、前置、班历、年龄、运输、终止和指标。
6. 上述全部通过后，再逐个加入真实状态、观测、重测/退出、故障/更换和正式随机实验。

## 7. G1 交接时的阶段门记录

- [x] A03 已重新签字：单次无条件为字面主情景、标准单工序链为关键替代；两口径固定各自 `alpha,beta`、全链独立重跑，只允许经验率偏离参照值。
- [x] 并行状态机、同工序重测再次异常、同刻完成先出结果、仅取消未完成任务及队列键已确认。
- [x] 测试、换新校准和装置周转均不可跨班，且只有能在本班完整结束才启动。
- [x] B19换班只改`on_duty_squad`；全局队列、原FCFS键、待重测首败记录、`release_time`和`effective_attempt=2`跨班跨队保留，且装置继续占台。
- [x] V3路线已复审签字，当前权威`CR-V3.1`注册表已完成；H1/R1为G2强制基线。
- [ ] 后续实现状态不在本冻结报告更新；统一见 `CURRENT_STATE.md`。
- [x] B05运输采用1h主情景、0.5h完全重叠替代情景。

G1、B18、B19 与 V3 复审均已签字。此后的任务完成度、正式运行和数字状态统一在 `CURRENT_STATE.md` 维护。
