# 归档区

本目录保存已失效或仅供追溯的材料。除非任务明确要求复盘某次决策，否则新对话、实现者和 checker 不得把这里作为当前输入。

当前子目录：

- `旧检查注册表/`：已被新版本取代的注册表；
- `历史评审/`：外部意见、响应矩阵和裁决过程；
- `旧模型方案/`：已被 `03_模型/` 当前文档取代的方案稿。

权威版本和阶段只看项目根 `CURRENT_STATE.md`。归档文件中的“当前”“唯一”“主情景”等措辞只描述其生成当时，不能覆盖当前契约。

不得修改归档内容来“同步现状”；需要纠错时在 `CHANGELOG.md` 或新版本中说明。

