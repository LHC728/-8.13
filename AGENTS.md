# 数学建模工作空间协作规则

## 作用域

本文件适用于本工作空间及其 `competitions/` 下全部竞赛项目。具体项目若有更严格的 `AGENTS.md`、`CURRENT_STATE.md` 或已签字问题契约，以更具体且更新的规则为准。

## 核心原则

1. 所有正式数字必须来自实际运行；聊天估算、手算审算例和调试输出不得进入论文结果。
2. 已冻结的题意、状态转移、指标和实验口径不得由实现者自行修改。
3. 遇到缺失、冲突或未定义规则必须停止相关实现并上报，不得静默补默认值。
4. 决策任务负责题意、公式、状态、目标、策略空间、实验设计和结论强度；实现任务只执行已冻结规格；独立复核任务不得照抄主实现。
5. 主模型与独立检查器必须分目录、分代码路径；可以共享只读参数和 schema，不得共享核心求根、状态转移、可行性或指标函数。
6. 论文中的公式、图表和数字必须能追溯到当前模型、配置、运行目录和检查报告；论文不能形成第二套手工模型或数字源。
7. 先完成可验证基线，再由证据决定是否增加复杂模型；不为了算法名称高级而堆叠方法。
8. 当前版本覆盖旧版本；历史文件保留用于追溯，但不得重新解释为当前口径。
9. 聊天记录和外部评审是输入证据，不是最终权威；决定必须落入项目文件才生效。
10. 所有项目产物必须留在对应项目目录内；不得把题目数据、代码、结果或论文散落到工作空间根目录。

## 进入竞赛项目时的默认阅读顺序

只先读取以下文件，不默认全文扫描历史评审：

1. 项目 `CURRENT_STATE.md`
2. 项目 `01_审计/问题契约.md`
3. `CURRENT_STATE.md` 指定的当前检查注册表
4. 项目 `02_数据/parameters.csv` 与当前运行配置
5. 项目 `03_模型/00_模型总览.md`

随后仅按当前 Gate 和任务需要读取项目计划、风险或专项模型文档。`99_归档/`、历史版本和 AI 使用日志默认不作为执行输入。

## 变更与阶段纪律

- 改变题意、概率语义、状态转移、指标分母、搜索空间或结论强度，必须先更新问题契约、当前状态和变更日志，并由决策层与队伍确认。
- 进入下一 Gate 前必须满足当前检查注册表的出口条件。
- 新增文件时先判断其职责：题意进审计、机器参数进数据、数学进模型、实现进代码、运行产物进结果、表达进论文、过程进项目管理。
- 不使用 `final2`、`最新最终版` 等含混命名。运行产物使用不可变 `run_id`，并绑定配置、代码、随机键、日志和检查报告。

## 当前竞赛入口

- `competitions/模拟赛_研究生A题/README.md`
- `competitions/模拟赛_研究生A题/CURRENT_STATE.md`

